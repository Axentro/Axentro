# This configuration file was generated by `ameba --gen-config`
# on 2019-05-30 00:06:22 UTC using Ameba version 0.9.1.
# The point is for the user to remove these configuration records
# one by one as the reported problems are removed from the code base.

# Problems found: 54
# Run `ameba --only Lint/UselessAssign` for details
Lint/UselessAssign:
  Description: Disallows useless variable assignments
  Enabled: true
  Excluded:
  - spec/units/dapps/transaction.cr
  - spec/units/dapps/fees.cr
  - spec/units/dapps/blockchain_info.cr
  - spec/units/dapps/token.cr
  - spec/units/dapps/rejects.cr
  - spec/units/dapps/utxo.cr
  - spec/units/dapps/indices.cr
  - spec/units/blockchain/transaction_decimal.cr
  - spec/units/blockchain/transaction.cr
  - spec/units/blockchain/transaction_pool.cr
  - spec/units/blockchain/block.cr
  - spec/units/node/rest_controller.cr
  - spec/units/node/rpc_controller.cr
  - src/core/dapps/build_in/scars.cr
  - src/core/node.cr
  - src/core/node/components/miners_manager.cr
  - src/core/node/components/chord.cr
  - src/core/client/client.cr
  - src/cli/sushi/client.cr

# Problems found: 3
# Run `ameba --only Performance/FirstLastAfterFilter` for details
Performance/FirstLastAfterFilter:
  FilterNames:
  - select
  Description: Identifies usage of `first/last/first?/last?` calls that follow filters.
  Enabled: true
  Excluded:
  - spec/units/dapps/blockchain_info.cr

# Problems found: 2
# Run `ameba --only Performance/SizeAfterFilter` for details
Performance/SizeAfterFilter:
  FilterNames:
  - select
  - reject
  Description: Identifies usage of `size` calls that follow filter
  Enabled: true
  Excluded:
  - spec/units/dapps/indices.cr
  - src/core/dapps/build_in/indices.cr

# Problems found: 1
# Run `ameba --only Style/RedundantReturn` for details
Style/RedundantReturn:
  Description: Reports redundant return expressions
  AllowMultiReturn: true
  AllowEmptyReturn: true
  Enabled: true
  Excluded:
  - e2e/utils/api.cr

# Problems found: 3
# Run `ameba --only Style/ConstantNames` for details
Style/ConstantNames:
  Description: Enforces constant names to be in screaming case
  Enabled: true
  Excluded:
  - src/core/dapps/build_in/scars.cr

# Problems found: 30
# Run `ameba --only Style/TypeNames` for details
Style/TypeNames:
  Description: Enforces type names in camelcase manner
  Enabled: true
  Excluded:
  - src/core/protocol/protocol.cr

# Problems found: 3
# Run `ameba --only Metrics/CyclomaticComplexity` for details
Metrics/CyclomaticComplexity:
  Description: Disallows methods with a cyclomatic complexity higher than `MaxComplexity`
  MaxComplexity: 10
  Enabled: true
  Excluded:
  - src/core/blockchain/block.cr
  - src/core/node.cr
  - src/cli/modules/global_optionparser.cr

# Problems found: 18
# Run `ameba --only Style/UnlessElse` for details
Style/UnlessElse:
  Description: Disallows the use of an `else` block with the `unless`
  Enabled: true
  Excluded:
  - src/core/node.cr
  - src/cli/sushi/transaction.cr
  - src/cli/sushi/scars.cr
  - src/cli/sushi/token.cr
  - src/cli/sushi/node.cr
  - src/cli/sushi/wallet.cr
  - src/cli/sushi/blockchain.cr
  - src/cli.cr

# Problems found: 2
# Run `ameba --only Lint/ShadowingOuterLocalVar` for details
Lint/ShadowingOuterLocalVar:
  Description: Disallows the usage of the same name as outer local variables for block
    or proc arguments.
  Enabled: true
  Excluded:
  - src/core/node/components/chord.cr
  - src/cli/sushi/client.cr

# Problems found: 6
# Run `ameba --only Layout/TrailingWhitespace` for details
Layout/TrailingWhitespace:
  Description: Disallows trailing whitespaces
  Enabled: true
  Excluded:
  - src/core/modules/welcome.cr

# Problems found: 1
# Run `ameba --only Style/RedundantBegin` for details
Style/RedundantBegin:
  Description: Disallows redundant begin blocks
  Enabled: true
  Excluded:
  - src/core/blockchain.cr
